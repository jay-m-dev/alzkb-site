server {
    listen 80;
    server_name $MYNGINX_SERVER_NAME;
    location / {
        proxy_set_header Host $host;
        proxy_pass http://$MYNGINX_SERVER_NAME:7474;
        proxy_redirect off;
    }
}
